# Scripts

- `scripts/repro_crl_antmaze_stitch.sh`: reproduce OGBench CRL baseline on the 4 AntMaze stitching datasets.
- `scripts/summarize_ogbench_csvs.py`: scan run folders and write `runs.csv` (+ optional aggregate) from `train.csv`/`eval.csv`/`flags.json`.
