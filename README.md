# Recurrent Offline RL (OGBench / CRL)

- Project plan: `PROJECT_PLAN.md`
- OGBench setup + run commands: `OGBENCH_SETUP.md`

This workspace vendors the official OGBench repo in `third_party/ogbench`, applies local patches, and now includes the SAW agent import from `johnlyzhou/saw`.
