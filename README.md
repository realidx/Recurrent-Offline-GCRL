# Recurrent Offline RL (OGBench / CRL)

- Project plan: `PROJECT_PLAN.md`
- OGBench setup + run commands: `OGBENCH_SETUP.md`

This workspace vendors the official OGBench repo in `third_party/ogbench` and is intended for experiments on CRL stitching tasks (e.g., `antmaze-large-stitch-v0`).

